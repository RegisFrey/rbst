// process tokens
// return the JSON like results, but preserving types

importFrom = 'rbst'

`
import type { TokenGroup } from '${importFrom}'

const tokens: TokenGroup = {
    ...
}

export default tokens;
`