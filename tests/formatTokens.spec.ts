describe ('formats tokens as CSS', () => {
    test ('with defaults', () => {
        expect(false).toBeTruthy();
    })
    test ('with provided seclector', () => {
        expect(false).toBeTruthy();
    })
    test ('for complex tokens', () => {
        expect(false).toBeTruthy();
    })
}

describe ('formats tokens as SCSS', () => {
    test ('with defaults', () => {
        expect(false).toBeTruthy();
    })
    test ('with provided seclector', () => {
        expect(false).toBeTruthy();
    })
    test ('as custom properties + variables', () => {
        expect(false).toBeTruthy();
    })
    test ('as custom properties only', () => {
        expect(false).toBeTruthy();
    })
    test ('for complex tokens', () => {
        expect(false).toBeTruthy();
    })
}

describe ('formats tokens as JSON', () => {
    test ('with defaults', () => {
        expect(false).toBeTruthy();
    })
    test ('for complex tokens', () => {
        expect(false).toBeTruthy();
    })
}